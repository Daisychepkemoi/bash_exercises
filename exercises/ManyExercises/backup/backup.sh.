#! /bin/bash

# //Self-reproducing Script
# //Write a script that backs itself up, that is, copies itself to a file named backup.sh.

ls

echo "LIST SUCCESSFULLY"
mkdir backup
#base_name=$(basename "$0")

cat self_reproducing.sh > backup/backup.sh.
cat backup/backup.sh.
echo "filed backedupsuccessfully"